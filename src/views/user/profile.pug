extends ../base.pug
include ../mixins/video
block content
  div.profile__data
    img(src="/"+user.avatarUrl, crossorigin).avatar
    h3=user.name
  div.video-grid
    each vid in user.video 
      +video(vid)
    else
      span.empty__message NOTHING FOUND.